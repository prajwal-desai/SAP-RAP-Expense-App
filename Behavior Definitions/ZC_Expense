projection;
strict ( 2 );
use draft;

define behavior for ZC_Expense alias Expense
{
  use create;
  use update;
  use delete;


  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;


  use action ApproveExpense;
  use action RejectExpense;
}
